"use client";

/**
 * Отключено: фиксированное меню Navbar уже содержит логотип
 * и ссылку на главную. Плавающая кнопка дублировала бы его
 * и перекрывала шапку. Компонент оставлен как заглушка,
 * чтобы не трогать SiteLayout.
 */
export function HomeLogo() {
  return null;
}
